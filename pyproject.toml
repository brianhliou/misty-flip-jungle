[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "jungle_flip_rust"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "jungle_flip_rust"
